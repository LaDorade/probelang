
main :: () {
    yop("bijour", 32);
    {
        let c = 22;
    }
    bijour :: () {
        let a = 23;
    }
    let b = 23;
}

yop :: (
    hop: string,
    yop: i32,
) {
    let x = y(aled(b, hop), y * 52, hop(23));
    print(x, y, e);
}
